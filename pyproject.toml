[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "nexah-orion"
version = "1.0.0"
description = "Certified deterministic Orientation Core for the NEXAH ecosystem"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
