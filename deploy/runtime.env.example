# Install as /etc/orion/runtime.env with mode 0600 and root ownership.
# Replace the example value before starting the service.
ORION_SERVICE_CREDENTIALS_JSON={"nexahedron":"replace-with-rotatable-secret"}
